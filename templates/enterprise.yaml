name: "Enterprise Engineering"
template: "enterprise"
created_at: "TEMPLATE_DATE"

spark:
  depth: "deep"
  ceo_mode: "expansion"

design:
  reviews:
    ceo: true
  specs:
    - id: ucd
      skill: "vibeflow-ucd"
      required: false
      skip_when: "no-ui"
    - id: design
      skill: "vibeflow-design"
      required: true

tasks:
  skill: "vibeflow-tasks"
  required: true
  task_block_minutes:
    min: 2
    max: 5

build:
  init_skill: "vibeflow-build-init"
  work_skill: "vibeflow-build-work"
  steps:
    - id: tdd
      skill: "vibeflow-tdd"
      required: true
    - id: quality
      skill: "vibeflow-quality"
      required: true
      gates:
        line_coverage: 95
        branch_coverage: 85
        mutation_score: 85
    - id: feature-st
      skill: "vibeflow-feature-st"
      required: true
    - id: review
      skill: "vibeflow-spec-review"
      required: true

review:
  required: true
  codex: true

test:
  st:
    skill: "vibeflow-test-system"
    required: true
  qa:
    required: false
    skip_when: "no-ui"
  design_review:
    required: false
    skip_when: "no-ui"

ship:
  version_strategy: "semver"
  create_pr: true
  create_tag: true
  document_release: true

reflect:
  required: true
  metrics:
    - "one_pass_rate"
    - "defect_density"
    - "coverage_trend"

quality_gates:
  line_coverage: 95
  branch_coverage: 85
  mutation_score: 85
