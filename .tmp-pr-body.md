## Summary
- consolidate the install surface down to the supported host installers and remove redundant Claude variants
- add Codex installers and move the OpenCode installer under `opencode/` for clearer host-specific layout
- rewrite the README install guidance into a compact host/platform matrix and add regression checks for the installer surface

## Testing
- pytest tests/test_project_version.py -q
